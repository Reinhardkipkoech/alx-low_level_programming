#include "main.h"
/**
 * print_last_digit - function that prints last digit
 * @int: integers
 *
 * Returns: value of last digit or 0
 */
int print_last_digit(int n)
{
	int m;

	m = n % 10;
	return (m);
}
